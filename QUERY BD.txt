create database DBPrueba;
go
use DBPrueba;
go

create table Usuario(
    IdUsuario int primary key identity,
    Nombre varchar(50),
    Correo varchar(50),
    Clave varchar(100),
    Estado char(1) default 'A', 
    FechaCreacion datetime default getdate(),
    UsuarioCreacion varchar(50) default 'Sistema', 
    FechaModificacion datetime null,
    UsuarioModificacion varchar(50) null
);
go

create table Producto(
    IdProducto int primary key identity,
    Nombre varchar(50),
    Marca varchar(50),
    Precio decimal(10,2),
    Estado char(1) default 'A',
    FechaCreacion datetime default getdate(),
    UsuarioCreacion varchar(50) default 'Sistema', 
    FechaModificacion datetime null,
    UsuarioModificacion varchar(50) null
);
go

insert into Producto
(Nombre,
Marca,
Precio)
values
(
'Laptop gamer 1001',
'HP',
3500
),
(
'Monitor curvo HD',
'Samsung',
2000
)

SELECT NEWID()
SELECT LEN('B016924A-DE13-4CA2-A828-B0ED7F9D9EB8')

-- Scaffold-DbContext "Data Source =PCIRDESA030\SQLEXPRESS;Initial Catalog=DBPrueba;Integrated Security=True;Trusted_Connection=True;TrustServerCertificate=True;" Microsoft.EntityFrameworkCore.SqlServer -OutPutDir Models